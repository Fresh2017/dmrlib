language: c

compiler:
  - clang
  - gcc

addons:
  apt:
    packages:
      - libtalloc-dev
      - libpcap-dev
      - portaudio-dev

cache: apt

script:
  - export CC=$CC
  - ./configure && make && make test || (cat config.log && false)
